# איך להעלות את האתר לאוויר (כתובת אינטרנט אמיתית)

המדריך הזה משתמש ב‑**Render** — יש לו תוכנית חינמית, Postgres מובנה, והוא בונה
את האתר ישירות מתוך ה‑repo ב‑GitHub. בסוף תהיה לך כתובת כמו
`https://amir-memorial.onrender.com`.

> צריך פעם אחת: חשבון GitHub (חינם) וחשבון Render (חינם).

---

## שלב 1 — להעלות את הקוד ל‑GitHub
הקוד כבר נמצא ב‑repo שלך. ודאי שהוא דחוף ל‑GitHub (לא לכלול את קובץ ה‑`.sql`).

## שלב 2 — ליצור את השירותים ב‑Render (Blueprint)
1. להיכנס ל‑https://dashboard.render.com ולהתחבר עם GitHub.
2. ללחוץ **New → Blueprint**.
3. לבחור את ה‑repo של האתר. Render יזהה אוטומטית את הקובץ `render.yaml`
   ויציע ליצור:
   - **amir-memorial** — שירות האתר (נבנה מה‑Dockerfile)
   - **amir-db** — מסד PostgreSQL
4. ללחוץ **Apply**. Render יבנה ויעלה (כמה דקות בפעם הראשונה).

בשלב הזה האתר כבר באוויר — אבל עם **מסד ריק**, אז צריך לטעון את הנתונים פעם אחת.

## שלב 3 — לטעון את הנתונים פעם אחת
1. ב‑Render, להיכנס ל‑מסד **amir-db** → להעתיק את **External Database URL**.
2. במחשב שלך (צריך `psql` מותקן — מגיע עם PostgreSQL), להריץ מתוך התיקייה
   שבה נמצא קובץ הדאטהבייס:

   ```bash
   psql "<ה-External Database URL שהעתקת>" < databasedump20260609.sql
   ```

   (שגיאות על `neon_superuser` — להתעלם, הן לא משפיעות על הנתונים.)

3. לרענן את כתובת האתר — התוכן שלך יופיע. 🎉

> אם אין לך `psql` במחשב — אפשר לבקש מ‑Claude Code שרץ אצלך במחשב לעשות את
> שלב 3, או להשתמש בכלי כמו TablePlus / DBeaver עם אותה כתובת.

---

## הערות חשובות
- **התצוגה הציבורית עובדת** (דף הבית, ביוגרפיה, תמונות, ספר אורחים, אנגלית).
- **אזור הניהול `/admin` עדיין לא יעבוד** — ההתחברות בנויה על Floot OAuth שלא
  פועל מחוץ ל‑Floot. כדי לערוך תוכן באתר עצמאי צריך להוסיף התחברות עם סיסמה.
- בתוכנית החינמית של Render האתר "נרדם" אחרי חוסר פעילות, והכניסה הראשונה
  אחרי שינה לוקחת כ‑30 שניות. לאתר קבוע אפשר לשדרג לתוכנית בתשלום נמוך.
- קובץ הדאטהבייס מכיל מידע אישי — לא להעלות אותו ל‑GitHub ולא לשתף בפומבי.
